# main-website

Phase 3A of the Solfaghari digital system. This is a local/dev scaffold only for now; see `../docs/main-tasks/brand-site-dev.md` for context.

Run `npm install` and then `npm run dev` inside this folder to start Eleventy.

Content and design are intentionally placeholder at this stage.
